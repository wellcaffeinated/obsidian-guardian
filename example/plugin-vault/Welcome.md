# Welcome

This is a throwaway vault used to test the Obsidian Guardian **plugin** inside
the headless Obsidian container. It is reset to its committed state before and
after each `pnpm test:plugin` run.
